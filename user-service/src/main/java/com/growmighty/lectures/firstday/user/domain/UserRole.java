package com.growmighty.lectures.firstday.user.domain;

/** USER: 후원자(기본값) / CREATOR: 판매자 등록 완료 / ADMIN: 관리자. CREATOR/ADMIN 도 후원 기능을 전부 쓸 수 있다. */
public enum UserRole {
    USER,
    CREATOR,
    ADMIN
}
