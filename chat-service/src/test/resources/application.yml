# 테스트 전용 설정 - @SpringBootTest 통합 테스ㅌ는 config-server/discovery-server 없이 독립 실행되어야 한다.
spring:
  application:
    name: chat-service
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
  ai:
    openai:
      api-key: test-key-not-a-real-key

eureka:
  client:
    enabled: false
