# Gradle's own task-level build cache (distinct from the Docker/GHA layer cache in
# Dockerfile): lets a task be skipped when its inputs already match a previous run's
# outputs, e.g. via gradle/actions/setup-gradle's persisted cache in ci.yml.
org.gradle.caching=true
