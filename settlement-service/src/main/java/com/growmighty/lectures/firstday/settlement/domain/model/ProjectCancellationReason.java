// TODO(settlement-plan): Map FAILED and CANCELLED project outcomes to batch refund reasons without Payment-specific reason codes.
package com.growmighty.lectures.firstday.settlement.domain.model;

public enum ProjectCancellationReason {
    PROJECT_FAILED,
    PROJECT_CANCELLED
}
